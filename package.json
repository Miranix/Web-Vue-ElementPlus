{
  "name": "{{name}}",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "author": {
    "name": "{{author}}",
    "email": "{{email}}"
  },
  "main": "index.js",
  "scripts": {
    "start": "nx run web-renderer:start",
    "build": "nx run web-renderer:build"
  },
  "dependencies": {
  },
  "workspaces": [
    "web-renderer"
  ]
}
